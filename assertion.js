
let word = "navi"
function shouter() {
    // return true
    // return str.toUpperCase() + "!";
}

console.assert(shouter !== undefined, "the shouter function needs to be defined")
var a = 25;
var b= 25;
console.assert(a + b === 50, "we need a + b to equal 50");

console.assert("real" === "real", "scream")
//.