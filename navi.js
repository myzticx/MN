console.assert(
  helloReturn("vince") === "hello vince",
  "make sure the name is returned with a heloo"
);

console.assert(
  helloReturn("pinkie") === "hello pinkie",
  "make sure the name is returned with a heloo"
);

console.assert(
  Divide(6, 2) === 3,
  "make sure the first paramter divded by the second is returned"
);
console.assert(
  add(2, 3) === 5,
  "make sure they are defined and can add together to 30"
);
console.assert(
  c + wot === 30,
  "make sure they are defined and can add together to 30"
);
