//manmohn test
console.assert(
  StringLengthCheck("five") === 4,
  "make sure the word length is returned"
);
console.assert(
  multiply(2, 3) === 6,
  "make sure they can multiply together to be 6"
);

console.assert(
  word1 + word2 === "completeword",
  "make sure words added together form complete word"
);

console.assert(
  addMany(1, 1, 1, 1, 1) === 5,
  "make sure they are defined and can add together to 30"
);
